rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Rules are wide open for any authenticated user.
    // This is not recommended for production applications.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
